# Unpinned on purpose: Streamlit Cloud runs Python 3.14, which has no wheels for
# the versions the model was trained under, so pinned versions compile from
# source and time out. The model ships as XGBoost JSON rather than a pickle, so
# it loads correctly on current library versions.
streamlit
pandas
numpy
scikit-learn
xgboost
